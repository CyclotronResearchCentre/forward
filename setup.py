from distutils.core import setup
import setuptools

setup(
    name='Forward',
    version='0.1dev',
    packages=['forward',],
    license='GNU GPL v2',
    long_description=open('README.md').read(),
)